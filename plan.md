# Beethoven — План автоматизации анализа продаж

## Обзор

Система автоматизирует процесс анализа записей пробных уроков и продаж в музыкальной студии Beethoven (филиалы: Астана, Усть-Каменогорск).

**Три компонента:**
1. **Telegram-бот** — сотрудники загружают аудиозаписи
2. **Backend** — транскрибация и AI-анализ
3. **Админ-панель** — руководство просматривает результаты и аналитику

---

## Стек технологий

| Компонент | Технология | Стоимость |
|-----------|-----------|-----------|
| БД + хранилище аудио | Supabase (free tier) | $0 |
| Frontend | React + CSS + JavaScript, деплой на Vercel | $0 |
| Backend | Python + FastAPI + Pydantic на VPS | ~$4-5/мес |
| TG-бот | python-telegram-bot (aiogram) на том же VPS | $0 |
| Транскрибация (STT) | Бесплатная мультимодальная модель на OpenRouter (временно) | $0 |
| AI-анализ | Бесплатная модель на OpenRouter | $0 |

**Итого: ~$5/мес** (только VPS)

---

## Схема базы данных (Supabase / PostgreSQL)

### employees
| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid, PK | |
| telegram_id | bigint, unique | ID в Telegram |
| name | text | Имя сотрудника |
| role | enum | `teacher` / `sales_manager` |
| city | enum | `astana` / `ust_kamenogorsk` |
| directions | text[] | Только для преподов: `guitar`, `piano`, `vocal`, `dombra` |
| created_at | timestamptz | |

### clients
| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid, PK | |
| name | text | Имя клиента |
| city | enum | Филиал |
| lesson_datetime | timestamptz | Дата и время пробного урока |
| result | enum | `bought` / `not_bought` / `prepayment` |
| created_at | timestamptz | |

**Уникальный ключ:** `(name, city, lesson_datetime)` — для связки записей препода и МОПа по одному клиенту.

### recordings
| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid, PK | |
| client_id | uuid, FK → clients | |
| employee_id | uuid, FK → employees | Кто загрузил |
| audio_path | text | Путь в Supabase Storage |
| transcription | text | Результат STT |
| analysis | text | Результат AI-анализа |
| score | smallint | Оценка 1-10 |
| status | enum | `pending` / `transcribing` / `analyzing` / `done` / `error` |
| created_at | timestamptz | |

### settings
| Поле | Тип | Описание |
|------|-----|----------|
| key | text, PK | Напр. `prompt_teacher`, `prompt_sales`, `admin_password` |
| value | text | Содержимое настройки |
| updated_at | timestamptz | |

> У клиента может быть 0-2 записи: одна от преподавателя, одна от МОПа. Обе привязаны через `client_id`.

---

## Этап 1. Инициализация проекта

- [ ] Создать монорепо с папками: `bot/`, `backend/`, `frontend/`
- [ ] Настроить Supabase проект: создать таблицы, storage bucket для аудио
- [ ] Настроить Python-окружение (venv, requirements.txt), общие Pydantic-модели

---

## Этап 2. Backend API

**Фреймворк:** FastAPI (Python)
**Валидация:** Pydantic
**Клиент БД:** supabase-py

### API эндпоинты

**Для бота (внутренние):**
- `POST /api/employees` — регистрация сотрудника
- `GET /api/employees/{telegram_id}` — получить профиль
- `POST /api/recordings` — загрузить запись (аудио + метаданные)
- `GET /api/recordings/{id}/status` — статус обработки

**Для админ-панели:**
- `POST /api/auth/login` — вход по паролю
- `GET /api/clients?city=&week=` — клиенты с записями для канбан-доски
- `GET /api/clients/{id}` — детали клиента с записями, транскрипциями, анализами
- `GET /api/analytics?city=&period=` — данные для раздела аналитики
- `GET /api/settings` — получить настройки (промпты)
- `PUT /api/settings/{key}` — обновить настройку

### Фоновая обработка

При загрузке аудио запускается pipeline:

```
1. Аудио загружается в Supabase Storage
2. Статус → transcribing
3. Аудио отправляется на OpenRouter (мультимодальная модель) → текст
4. Статус → analyzing
5. Транскрипция + промпт (teacher/sales в зависимости от роли) → OpenRouter → анализ + оценка
6. Результат сохраняется в БД, статус → done
```

**Очередь:** `asyncio.Queue` или `BackgroundTasks` от FastAPI — Redis не нужен при 10 записях/день.

### Промпты (начальные, редактируемые через админку)

**Промпт для преподавателя** — оценка по критериям:
- Прогрев клиента
- Создание вау-эффекта
- Подготовка к продаже
- Вовлечение клиента
- Общее впечатление

**Промпт для МОПа** — оценка по критериям:
- Выявление потребностей
- Работа с возражениями
- Презентация продукта
- Закрытие сделки
- Общее качество продажи

Оба промпта должны возвращать: структурированный анализ по пунктам + итоговую оценку от 1 до 10.

---

## Этап 3. Telegram-бот

**Библиотека:** aiogram 3
**Запуск:** отдельный процесс на том же VPS (или в одном процессе с FastAPI через asyncio)

### Флоу регистрации (первый запуск)

```
/start
→ "Как вас зовут?" (ввод текста)
→ "Выберите город:" [Астана] [Усть-Каменогорск]
→ "Ваша должность:" [Преподаватель] [Менеджер отдела продаж]
→ (если препод) "Выберите направления:"
   Астана: [Гитара] [Фортепиано] [Вокал] [Домбра]
   Усть-Каменогорск: [Гитара] [Фортепиано] [Вокал]
→ "Регистрация завершена!"
```

### Флоу отправки записи

**Вариант А — пошаговый (по умолчанию):**
```
Сотрудник отправляет /new или нажимает кнопку "Новая запись"
→ "Отправьте аудиозапись"
→ (аудио получено) "Укажите дату и время урока (ДД.ММ.ГГГГ ЧЧ:ММ)"
→ "Имя клиента?"
→ "Результат:" [Купил] [Не купил] [Предоплата]
→ "Запись принята! Обработка займёт несколько минут."
```

**Вариант Б — одним сообщением:**
```
Сотрудник отправляет аудио с подписью:
"03.05.2025 15:00 Иванов Иван купил"

Бот парсит подпись и принимает запись.
→ "Запись принята! Обработка займёт несколько минут."

Если формат подписи не распознан → бот переключается на пошаговый флоу.
```

### Команды
- `/start` — регистрация
- `/new` — новая запись (пошаговый флоу)
- `/profile` — просмотр/изменение профиля
- `/status` — статус последних записей

---

## Этап 4. Админ-панель (Frontend)

**Стек:** React + JavaScript + CSS (без TypeScript, без UI-фреймворков)
**Сборка:** Vite
**Графики:** Recharts
**Деплой:** Vercel (бесплатно)

### Авторизация

Простой общий пароль. Хранится в таблице `settings` (ключ `admin_password`). На фронте — страница логина с одним полем "пароль". При успешном входе — JWT-токен (PyJWT на бэке).

### Макет

```
┌─────────────────────────────────────────────────────────┐
│  BEETHOVEN                           [Астана ▼]         │
├───────────┬─────────────────────────────────────────────┤
│           │                                             │
│  Анализы  │  ◄  19 - 25 мая 2025  ►  📅                │
│           │                                             │
│ Аналитика │  Пн 19    Вт 20    Ср 21    Чт 22   ...   │
│           │ ┌───────┐ ┌───────┐          ┌───────┐     │
│ Настройки │ │ 15:00 │ │ 10:00 │          │ 12:00 │     │
│           │ │ Анна К│ │Борис М│          │Дарья Л│     │
│           │ │ Клиент│ │ Клиент│          │ Клиент│     │
│           │ │ 🟢    │ │ 🔴    │          │ 🟡    │     │
│           │ └───────┘ └───────┘          └───────┘     │
│           │ ┌───────┐                                   │
│           │ │ 17:00 │                                   │
│           │ │Мария С│                                   │
│           │ │ Клиент│                                   │
│           │ │ 🟢    │                                   │
│           │ └───────┘                                   │
└───────────┴─────────────────────────────────────────────┘
```

### Страницы

#### 1. Анализы (канбан-доска)

- 7 колонок = дни текущей недели (Пн–Вс)
- Карточка клиента содержит: время, имена препода и МОПа, имя клиента, цветовой индикатор результата (зелёный/красный/жёлтый)
- `◄ ►` — навигация по неделям
- 📅 — date picker для перехода к конкретной дате
- Фильтр по городу в хедере

#### 2. Детальная карточка (модалка по клику)

- Имя клиента, дата, время
- Результат: купил / не купил / предоплата
- **Блок преподавателя** (если есть запись):
  - Имя, направление
  - Оценка: X/10
  - Анализ (структурированный текст)
  - Транскрипция (сворачиваемая)
- **Блок МОПа** (если есть запись):
  - Имя
  - Оценка: X/10
  - Анализ
  - Транскрипция (сворачиваемая)

#### 3. Аналитика

- Фильтры: город, период (неделя/месяц/произвольный), сотрудник
- **Конверсия:** круговая диаграмма — купил / не купил / предоплата
- **Топ-3 лучших продаж** (по оценке) — карточки со ссылкой на анализ
- **Топ-3 худших продаж** — аналогично
- **Частые ошибки** — агрегированный список на основе AI-анализов

#### 4. Настройки

- Редактор промпта для преподавателей (textarea)
- Редактор промпта для МОПов (textarea)
- Смена пароля админки

---

## Этап 5. Деплой

- **VPS** (Hetzner/Timeweb, ~$5/мес): FastAPI backend + aiogram бот + Nginx + Uvicorn
- **Vercel** (бесплатно): React frontend
- **Supabase** (free tier): БД + storage
- SSL через Let's Encrypt

---

## Этап 6. Тестирование и запуск

1. Тестирование бота: регистрация, оба варианта отправки записей
2. Тестирование STT на реальных записях (проверить качество бесплатной модели)
3. Настройка промптов под реальные кейсы
4. Тестирование админ-панели: канбан, карточки, аналитика
5. Пилот на одном филиале
6. Обратная связь → доработка
7. Запуск на оба филиала

---

## Порядок разработки

| # | Задача | Зависит от |
|---|--------|------------|
| 1 | Supabase: таблицы, storage, настройка | — |
| 2 | Backend: API + Supabase клиент | 1 |
| 3 | Telegram-бот: регистрация + отправка аудио | 2 |
| 4 | Backend: pipeline STT + LLM (OpenRouter) | 2 |
| 5 | Frontend: авторизация + канбан-доска | 2 |
| 6 | Frontend: детальная карточка анализа | 5 |
| 7 | Frontend: раздел аналитики | 5 |
| 8 | Frontend: страница настроек (редактор промптов) | 5 |
| 9 | Деплой на VPS + Vercel | 2-8 |
| 10 | Тестирование + запуск | 9 |

---

## Направления по филиалам

**Астана:** Гитара, Фортепиано, Вокал, Домбра
**Усть-Каменогорск:** Гитара, Фортепиано, Вокал
